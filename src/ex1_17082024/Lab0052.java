package ex1_17082024;

public class Lab0052 {

    // wrapper class - whose objects wraps or contains primitive data types
    // They convert primitive data types into objects
    // Data structures in collection framework, such as arraylist, vector, hashmap - they don't support primitive data type

    // in api, web automation -> we avoid primitive
    // Automation -> class and objects - wrapper classes

    //Wrapper -> extra functions or utilities
    // Character, Byte, Short, Long, Integer, Float, Double,Boolean

    // Why wrapper clas?
    // Collection framework we need to use wrapper class
    // Storing null values. Object -> can be null
    // Converting primitive data types n strings
    // perform mathematical operations



    public static void main(String[] args) {
        Integer a = 100;
        a.compareTo(10);  // built-in functions
    }
}
